// 文件属性读写。
// 其中常用的有fs.stat(获取文件信息，包括字节数等)、fs.chmod（该方法以异步的方式来改写文件的读写权限。）、fs.chown（更改文件所有权。）等等。
// 文件内容读写。
// 其中常用的有fs.readFile、fs.readdir、fs.writeFile、fs.mkdir等等。
// 底层文件操作。
// 其中常用的有fs.open、fs.read、fs.write、fs.close等等。
